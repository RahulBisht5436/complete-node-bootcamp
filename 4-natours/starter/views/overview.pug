extends base

block content
  main.main
    .card-container
      each cardData in data
        .card
          .card__header
            .card__picture
              .card__picture-overlay &nbsp;
              img.card__picture-img(src=`img/tours/${cardData.imageCover}`, alt=cardData.name)

            h3.heading-tertirary
              span #{cardData.name} 

          .card__details
            h4.card__sub-heading= `${cardData.difficulty} ${cardData.duration}-day tour`
            p.card__text= cardData.summary

            .card__data
              svg.card__icon
                use(xlink:href="img/icons.svg#icon-map-pin")
              span #{cardData.startLocation.description}

            .card__data
              svg.card__icon
                use(xlink:href="img/icons.svg#icon-calendar")
              span= new Date(cardData.startDates[0]).toLocaleString('en-US', {month: 'long',year: 'numeric'})


            .card__data
              svg.card__icon
                use(xlink:href="img/icons.svg#icon-flag")
              span= `${cardData.locations.length} stops`

            .card__data
              svg.card__icon
                use(xlink:href="img/icons.svg#icon-user")
              span= `${cardData.maxGroupSize} People`
          
          .card__footer
            .card__footer_text
              p
                span.card__footer-value $#{cardData.price}
                |
                span.card__footer-text per person
              if cardData.ratingQuantity > 0
                p.card__ratings
                  span.card__footer-value #{cardData.ratingAverage}
                  |
                  |
                  span.card__footer-text rating (#{cardData.ratingQuantity})
            if cardData.slug 
              .card__footer_btn
                a.btn.btn--green.btn--small(href=`/tours/${cardData.slug}`) Details

